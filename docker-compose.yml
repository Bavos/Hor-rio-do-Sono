services:
  app:
    image: node:22-bookworm-slim
    working_dir: /app
    command: sh -c "npm install && npm run backend:dev"
    ports:
      - "3333:3333"
    env_file:
      - .env
    volumes:
      - .:/app
